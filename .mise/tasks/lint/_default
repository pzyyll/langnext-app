#!/usr/bin/env bash
# ABOUTME: Run ESLint (authoritative) then oxlint (fast correctness companion)
# ABOUTME: Run via `mise run lint`
#MISE description="Run ESLint and oxlint"
set -euo pipefail
eslint .
oxlint .
