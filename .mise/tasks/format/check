#!/usr/bin/env bash
# ABOUTME: Check oxfmt and rustfmt without writing files
# ABOUTME: Run via `mise run format:check`
#MISE description="Check formatting (oxfmt + rustfmt)"
set -euo pipefail
oxfmt --check
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
