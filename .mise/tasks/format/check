#!/usr/bin/env bash
# ABOUTME: Check Prettier and rustfmt without writing files
# ABOUTME: Run via `mise run format:check`
#MISE description="Check formatting (Prettier + rustfmt)"
set -euo pipefail
prettier --check .
cargo fmt --manifest-path src-tauri/Cargo.toml -- --check
