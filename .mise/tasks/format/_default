#!/usr/bin/env bash
# ABOUTME: Format frontend (oxfmt) and Rust (cargo fmt)
# ABOUTME: Run via `mise run format`
#MISE description="Format sources (oxfmt + rustfmt)"
set -euo pipefail
oxfmt
cargo fmt --manifest-path src-tauri/Cargo.toml
