// ABOUTME: Registers jest-dom matchers for Bun component tests.
// ABOUTME: Imported explicitly by DOM test modules after registerDom, so matchers bind with a real document.
import "@testing-library/jest-dom";
