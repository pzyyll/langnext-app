# ABOUTME: Project toolchain versions managed by mise
# ABOUTME: Project tasks live in `.mise/tasks/` as file tasks

[tools]
node = "26.3.0"
bun = "1.3"
rust = "stable"

[env]
_.path = ["{{config_root}}/node_modules/.bin"]
